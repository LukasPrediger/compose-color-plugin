object ColorSchemes {
    val lightColorScheme: ColorScheme =
        ColorScheme(
            primary = Colors.Primary.tone40,
            onPrimary = Colors.Primary.tone100,
            primaryContainer = Colors.Primary.tone90,
            onPrimaryContainer = Colors.Primary.tone10,
            inversePrimary = Colors.Primary.tone80,
            secondary = Colors.Secondary.tone40,
            onSecondary = Colors.Secondary.tone100,
            secondaryContainer = Colors.Secondary.tone90,
            onSecondaryContainer = Colors.Secondary.tone10,
            tertiary = Colors.Tertiary.tone40,
            onTertiary = Colors.Tertiary.tone100,
            tertiaryContainer = Colors.Tertiary.tone90,
            onTertiaryContainer = Colors.Tertiary.tone10,
            background = Colors.Neutral.tone99,
            onBackground = Colors.Neutral.tone10,
            surface = Colors.Neutral.tone98,
            onSurface = Colors.Neutral.tone10,
            surfaceVariant = Colors.NeutralVariant.tone90,
            onSurfaceVariant = Colors.NeutralVariant.tone30,
            surfaceTint = Colors.Primary.tone40,
            inverseSurface = Colors.Neutral.tone20,
            inverseOnSurface = Colors.Neutral.tone95,
            error = Colors.Error.tone40,
            onError = Colors.Error.tone100,
            errorContainer = Colors.Error.tone90,
            onErrorContainer = Colors.Error.tone10,
            outline = Colors.NeutralVariant.tone50,
            outlineVariant = Colors.NeutralVariant.tone80,
            scrim = Colors.Neutral.tone0,
            surfaceBright = Colors.Neutral.tone98,
            surfaceContainer = Colors.Neutral.tone94,
            surfaceContainerHigh = Colors.Neutral.tone92,
            surfaceContainerHighest = Colors.Neutral.tone90,
            surfaceContainerLow = Colors.Neutral.tone96,
            surfaceContainerLowest = Colors.Neutral.tone100,
            surfaceDim = Colors.Neutral.tone87,
        )

    val darkColorScheme: ColorScheme =
        ColorScheme(
            primary = Colors.Primary.tone80,
            onPrimary = Colors.Primary.tone20,
            primaryContainer = Colors.Primary.tone30,
            onPrimaryContainer = Colors.Primary.tone90,
            inversePrimary = Colors.Primary.tone40,
            secondary = Colors.Secondary.tone80,
            onSecondary = Colors.Secondary.tone20,
            secondaryContainer = Colors.Secondary.tone30,
            onSecondaryContainer = Colors.Secondary.tone90,
            tertiary = Colors.Tertiary.tone80,
            onTertiary = Colors.Tertiary.tone20,
            tertiaryContainer = Colors.Tertiary.tone30,
            onTertiaryContainer = Colors.Tertiary.tone90,
            background = Colors.Neutral.tone10,
            onBackground = Colors.Neutral.tone90,
            surface = Colors.Neutral.tone10,
            onSurface = Colors.Neutral.tone90,
            surfaceVariant = Colors.NeutralVariant.tone30,
            onSurfaceVariant = Colors.NeutralVariant.tone80,
            surfaceTint = Colors.Primary.tone80,
            inverseSurface = Colors.Neutral.tone90,
            inverseOnSurface = Colors.Neutral.tone20,
            error = Colors.Error.tone80,
            onError = Colors.Error.tone20,
            errorContainer = Colors.Error.tone30,
            onErrorContainer = Colors.Error.tone90,
            outline = Colors.NeutralVariant.tone60,
            outlineVariant = Colors.NeutralVariant.tone30,
            scrim = Colors.Neutral.tone0,
            surfaceBright = Colors.Neutral.tone24,
            surfaceContainer = Colors.Neutral.tone12,
            surfaceContainerHigh = Colors.Neutral.tone17,
            surfaceContainerHighest = Colors.Neutral.tone22,
            surfaceContainerLow = Colors.Neutral.tone10,
            surfaceContainerLowest = Colors.Neutral.tone4,
            surfaceDim = Colors.Neutral.tone6,
        )
}
